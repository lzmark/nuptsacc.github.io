{
  "name": "test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npm run build && node-sass-chokidar style-src -o style --watch --recursive"
  },
  "devDependencies": {
    "node-sass-chokidar": "^0.0.3"
  }
}
